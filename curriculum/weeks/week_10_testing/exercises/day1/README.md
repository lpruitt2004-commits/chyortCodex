# Day 1: TS + Jest Fundamentals

Goals:
- Configure ts-jest
- Write tests for pure functions
- Practice TDD: red → green → refactor

Tasks:
1) Implement `sum(a, b)` in `sum.ts` with proper types.
2) Add input validation: only numbers, throw on invalid.
3) Implement `capitalize(str)` in `strings.ts` (empty/single/multi-word handling).

Run tests:
```bash
npm run day1
```
